import { Typography } from "@mui/material";

const Logo = () => {
  return (
    <>
      <Typography sx={{ color: "white" }} variant="h3">
        iStore
      </Typography>
    </>
  );
};

export default Logo;
